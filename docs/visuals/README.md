# Model-led documentation

## Capture notes

Actual browser captures of the existing static documentation in dark/light themes. The terminal is an illustrative command sequence, not an executed database operation. System fallback fonts were used in offline capture.

Web captures use Chromium at 1440 × 1080 (desktop) and 390 × 1080 (mobile), with reduced motion enabled. Screenshots are real rendered interfaces, not image-generated UI mockups. Raster renderer examples retain their native dimensions.

## Verification — 2026-09-14

`node --check docs/assets/main.js` passed. Offline browser checks cover five widths, theme switching, quickstart navigation, and keyboard table sorting. No models, containers, Oracle instances, or Python inference dependencies were installed or invoked.

Only the existing visual surfaces were changed. The screenshots are not evidence of end-to-end service availability, accessibility certification, or production performance.
